pipeline {
    agent any

    stages {
        stage ('Build') {
            steps {
                sh 'echo "Sábado de aulas intensivas"'
                sh '''
                    echo "Nosso sábado"
                    ls -la
                '''
            }
        }
    }
}